services:
  word-app-service:
    build: ./backend/.
    environment:
        - PORT=3000
  word-app-frontend:
    build: ./frontend/.
    ports:
      - 8080:80
    environment:
        - PORT=80
        - SERVICE_PORT=3000
        - SERVICE_HOSTNAME=word-app-service